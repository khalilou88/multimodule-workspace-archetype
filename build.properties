# Build configuration
# build.properties
# Build configuration for the archetype

# Archetype information
archetype.groupId=com.example.archetype
archetype.artifactId=multimodule-workspace-archetype
archetype.version=1.0.0
archetype.name=Multi-Module Workspace Archetype
archetype.description=Maven archetype for creating multi-module workspace projects

# Default project configuration
default.groupId=com.example
default.artifactId=my-project
default.version=1.0.0-SNAPSHOT
default.package=com.example

# Build settings
maven.compiler.source=17
maven.compiler.target=17
project.build.sourceEncoding=UTF-8